# Level 2: Sicherer Python-Bot

Bearbeite ausschließlich `student_bot.py`.

## Reihenfolge

1. Implementiere `is_blocked(snapshot, point)`.
2. Implementiere `safe_moves(snapshot)`.
3. Wähle in `choose_move(snapshot)` einen sicheren Zug.
4. Ergänze eigene Tests für Hindernisse und Sackgassen.

Ausführen:

```sh
python -m pytest tests/test_learning_level2.py
```

Die Datei ist absichtlich unvollständig. Die Referenzbots liegen getrennt im Simulator.
